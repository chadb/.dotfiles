Installation

cd $HOME
git clone git://github.com/chadb/.dotfiles.git
cd ~/.dotfiles
./link.sh 

VIM 
  I'm using vundle to deal with plugins. https://github.com/gmarik/Vundle.vim
  To install the vim bundle:

  vim +BundleInstall +qall
